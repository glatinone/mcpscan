name: post-build
on:
  workflow_run:
    workflows: [Build]
    types: [completed]
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Report build status
        run: echo "Build finished with conclusion ${{ github.event.workflow_run.conclusion }}"
